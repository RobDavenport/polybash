# Fixtures

Fixtures are the test truth sources for the first PolyBash milestone.

Use valid fixtures for happy-path tests.
Use invalid fixtures for typed error-path tests.

The goal is to keep acceptance evidence explicit and reproducible.

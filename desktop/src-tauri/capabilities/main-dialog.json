{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-dialog",
  "description": "Allow the main PolyBash window to use native open/save dialogs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open", "dialog:allow-save"]
}

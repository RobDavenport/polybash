# UI Panels

The first scaffold keeps UI deliberately thin.
Controllers and view-model seams are the priority.
Real Blockbench panels should be added only after the headless controller flow is stable.

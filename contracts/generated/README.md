# Generated Contracts

These files are starter schema artifacts for the scaffold.

Long-term rule:
- Rust contract types are the source of truth.
- schema generation should become automated.
- TypeScript runtime validation should consume these generated files.

Codex should replace manual starter schemas with a generated path during `T-01`.

# Prompt 04 — CLI

Target tasks:
- `T-07`

Focus:
- `validate`
- `inspect`
- `export`

Rules:
- CLI output must be script-friendly
- file paths and errors must be explicit
- export must write both GLB and report

Required deliverables:
- CLI integration tests
- documented commands in README or docs

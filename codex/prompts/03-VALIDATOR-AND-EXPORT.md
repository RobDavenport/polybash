# Prompt 03 — Validator and Export

Target tasks:
- `T-05`
- `T-06`

Focus:
- typed validation pipeline
- report generation
- deterministic export path
- fatal validation blocks export

Rules:
- validation errors must be explicit and stable
- no silent auto-correction
- export outputs must be deterministic
- report snapshots must be reviewable

Required deliverables:
- positive and negative validation tests
- report snapshot tests
- export tests on the canonical fighter fixture
